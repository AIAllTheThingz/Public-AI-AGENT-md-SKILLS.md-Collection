# 1.0.0-rc.1 Readiness Record

Status: **Prepared source candidate validated; publication and independent specialist approval remain pending.**

This record captures the repository-source readiness evidence for `1.0.0-rc.1`. It does not claim that the `v1.0.0-rc.1` tag or GitHub Release exists, and it does not claim final `1.0.0` readiness.

## Candidate identity

- Candidate version: `1.0.0-rc.1`
- Functional candidate commit: `89a0470a3be4902f0c70eb31d396655dde4c8d7c`
- Pull request: `#71` (`release: exercise 1.0.0-rc.1 compatibility gate`)
- Published migration checkpoint: `v0.10.0`
- Published checkpoint commit: `83c73f3ab9a049ff2321d463164fcf98fb453a9c`
- Compatibility inventory: `releases/compatibility/1.0.0-rc.1.json`
- Published checkpoint inventory: `releases/compatibility/0.10.0-checkpoint.json`
- Migration exercise: `releases/migrations/1.0.0-rc.1.md`

The functional candidate SHA above intentionally precedes this readiness record and later documentation-only commits. It is the immutable tree on which the compatibility gate and permanent validation were exercised.

## Permanent validation evidence

- GitHub Actions workflow: `Validate standards`
- Workflow run: `31973509196`
- Candidate commit validated: `89a0470a3be4902f0c70eb31d396655dde4c8d7c`
- Result: **Passed**
- Runtime boundary: Ubuntu 24.04, Python 3.13, hash-locked validation dependencies
- Command executed by permanent CI: `python tools/validate-all/run_all.py --include-tests`
- Validators completed: repository standards, links, source freshness, skills, schemas, templates, tools, and release validation
- Unit tests: **Passed**

The permanent unit suite includes the RC-specific regression that executes:

```bash
python tools/release/validate_release.py --format json --tag v1.0.0-rc.1
```

That contract validation passed on the functional candidate. It validates the release-tag contract only; it does not create or publish the tag.

## Compatibility evidence

Classification: **compatible relative to published `v0.10.0`.**

The RC compatibility gate:

- pins the published `v0.10.0` source boundary and immutable checkpoint inventory;
- preserves published root stable entry paths;
- preserves published rolling schema paths and the versioned major-1 schema contract used for long-lived consumers;
- preserves the seven original stable template paths;
- preserves documented stable tool entry points, tool-result contract, exit-code/JSON behavior, and release artifact naming/checksum contract;
- preserves canonical uppercase project-profile compatibility entry points;
- verifies all candidate-enumerated stable paths exist;
- includes a negative regression that deliberately removes a published stable path and requires the checkpoint comparison to fail;
- records no breaking change relative to published `v0.10.0`.

## Package maturity boundary

The initial stable package promise is intentionally narrow:

- `languages/csharp`: **stable**
- all packages not explicitly enumerated as stable: outside the `1.0.0` stable package promise for this candidate

PowerShell and Terraform/OpenTofu remain `baseline` under their recorded maturity decisions. Repository path compatibility does not imply stable package maturity.

## Migration exercise

Migration from published `v0.10.0` to this candidate was exercised as an additive compatibility review:

- no required breaking migration was identified;
- adopters evaluating the RC must pin the exact candidate source and rerun applicable validation;
- C# adopters must review the newly explicit stable commitment;
- Windows PowerShell 5.1 adopters must review the additive legacy overlay where applicable;
- mixed-profile adopters using `--include-profile-required` must review the additive secondary-profile discipline expansion;
- downgrade to `v0.10.0` remains possible by restoring the published source pin and removing RC-only maturity claims.

## Review findings

One P1 review finding on PR #71 identified circular compatibility evidence: the original candidate test only proved that paths listed by the candidate still existed. The remediation added an immutable `v0.10.0` checkpoint inventory, checkpoint-to-candidate comparison, profile/tool/release-contract coverage, and a negative stable-path-removal regression.

The review thread is resolved after the remediation and permanent validation passed.

This automated/repository review does **not** substitute for the independent specialist approval required by the repository release policy.

## Readiness state

| Gate | State | Evidence / limitation |
|---|---|---|
| Candidate version and notes | Passed | `VERSION`, `CHANGELOG.md`, `releases/1.0.0-rc.1.md` |
| Migration from published checkpoint | Passed | `releases/migrations/1.0.0-rc.1.md` and permanent regressions |
| Published checkpoint compatibility comparison | Passed | checkpoint inventory plus negative-removal regression |
| Permanent repository validation | Passed | run `31973509196` on `89a0470a...` |
| RC tag contract validation | Passed | `validate_release.py --tag v1.0.0-rc.1` in permanent unit suite |
| Stable-package boundary | Passed | C# stable; non-enumerated packages excluded from stable promise |
| Review finding remediation | Passed | PR #71 P1 thread resolved |
| Independent specialist approval for RC | `NotRun` | Required before treating the candidate as independently approved |
| Published `v1.0.0-rc.1` tag | `NotRun` | No publication claim in this source record |
| Published GitHub Release and attached artifacts | `NotRun` | Requires deliberate tag-driven publication |
| Independent checksum/artifact verification after publication | `NotRun` | Cannot occur before publication |
| Final `1.0.0` exact-commit compatibility approval | `NotRun` | Separate final-release gate |
| GitHub-host controls previously recorded as manual/Blocked | `Blocked` where previously recorded | Repository-source validation cannot configure or prove those controls |

## Final 1.0.0 boundary

Final `1.0.0` remains blocked until all policy gates are satisfied, including:

1. at least one `1.0.0-rc.N` is actually published and available for review;
2. release artifacts and `SHA256SUMS.txt` are independently verified after publication;
3. an independent specialist records compatibility approval on the exact final release commit;
4. no unresolved high/critical release blocker remains;
5. required source-review, security, adoption, maturity, ownership, and migration evidence remains current for the exact final candidate.

This readiness record establishes a validated source candidate. It does not convert a prepared RC into a published release and does not collapse the separate final-`1.0.0` approval gate.
