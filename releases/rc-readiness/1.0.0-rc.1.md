# 1.0.0-rc.1 Readiness Record

Status: **Functional compatibility candidate validated; exact record/document tree validation remains `NotRun` in this committed record; publication and exact-head independent specialist approval remain pending.**

This record captures repository-source compatibility evidence for `1.0.0-rc.1`. It does not claim that the `v1.0.0-rc.1` tag or GitHub Release exists, it does not claim final `1.0.0` readiness, and it does not claim that the commit containing this record has already been validated.

## Candidate identity

- Candidate version: `1.0.0-rc.1`
- Functional compatibility candidate commit: `d70a4d3d96c3f3691b021f74e2e8d22b36114821`
- Functional validation run: `31989888372`
- Pull request: `#71` (`release: exercise 1.0.0-rc.1 compatibility gate`)
- Published migration checkpoint: `v0.10.0`
- Published checkpoint commit: `83c73f3ab9a049ff2321d463164fcf98fb453a9c`
- C# immutable promotion-evidence candidate: `2f6d39288e5c1a7d416e62cd75651b3d6da48dfe`
- Compatibility inventory: `releases/compatibility/1.0.0-rc.1.json`
- Published path and identifier checkpoint: `releases/compatibility/0.10.0-checkpoint.json`
- Published agent-skill entry-point checkpoint: `releases/compatibility/0.10.0-agent-skill-entrypoints.json`
- Published normative-rule checkpoint: `releases/compatibility/0.10.0-rule-contracts.json`
- Published tool-behavior checkpoint: `releases/compatibility/0.10.0-tool-behavior.json`
- Published finding-code checkpoint: `releases/compatibility/0.10.0-finding-codes.json`
- Migration exercise: `releases/migrations/1.0.0-rc.1.md`

The functional candidate intentionally precedes this readiness-record commit. The complete compatibility gate was executed on that immutable tree. Validation of the later commit containing this record must be established by a subsequent exact-head CI run and carried in PR metadata rather than written back here and creating a self-referential head.

## Permanent validation evidence

### Functional compatibility candidate

- GitHub Actions workflow: `Validate standards`
- Workflow run: `31989888372`
- Candidate commit validated: `d70a4d3d96c3f3691b021f74e2e8d22b36114821`
- Result: **Passed**
- Runtime boundary: Ubuntu 24.04, Python 3.13, hash-locked validation dependencies
- Command: `python tools/validate-all/run_all.py --include-tests`
- Validators completed: repository standards, links, source freshness, skills, schemas, templates, tools, release validation, and unit tests

The permanent RC suite now verifies these compatibility boundaries:

- stable root, schema, template, tool-entry, profile-file, and profile-directory paths against immutable `v0.10.0` evidence;
- all seven agent-skill routes published by the `v0.10.0` root manifest, including `languages/SKILL.md`;
- published front-matter identifiers, schema/tool-result `$id` values, and numbered normative-rule occurrences as source path + rule ID + `Requirement` meaning;
- published scoped duplicate rule IDs exactly where released, while new cross-file or same-file reuse is rejected and new unique rules remain compatible additions;
- C# stable identifiers and ten numbered product-rule meanings;
- promoted C# `*_STANDARD.md` files by stable front-matter ID plus promoted normative Markdown contracts from immutable promotion evidence, allowing non-contract editorial corrections and additive compatible requirements without byte-for-byte directory identity;
- published CLI options and values, result and exit-code contracts, release artifact naming, and structural result-schema compatibility;
- both published output formats, `text` and `json`, across every common stable CLI parser;
- every stable writing tool published in `v0.10.0`, including `generate-manifest` and `compose-agents`, for overwrite refusal without `--force`, dry-run non-mutation, and replacement under `--force`;
- every published literal finding code as a source-derived semantic contract and as a reachable finding occurrence, so an unconditional preceding `return` or `raise` cannot silently suppress a published finding;
- caller-supplied finding codes by source path, caller identity, resolved non-code arguments, helper identity, and code value, including a negative `LICENSE_ENCODING` / `NOTICE_ENCODING` call-site swap regression;
- module-level parameterized-finding helpers themselves by alpha/message-normalized helper semantics, so behavior changes such as `read_text` catching the wrong exception fail while private parameter renames and message rewording remain compatible;
- behavior-driving constants, local dataflow, referenced helper implementations, code identity, and stable non-message emission shape while excluding human-readable message wording and helper docstrings;
- the intentional post-`v0.10.0` `read_release_state()` strengthening pinned to immutable reconciliation commit `a96d6a92da40257cbe4c6e0fe0c7bbbd397adef3` for only its two affected publication findings;
- both runtime outcomes of `git_output(...)` for `RELEASE_HEAD_TAG_MISSING`;
- `validate_release.py --tag v1.0.0-rc.1` as a permanent RC-tag regression;
- negative regressions for stable-path removal, agent-skill-route removal, identifier mutation/removal, normative-rule removal/meaning/reuse, finding reachability loss, caller-code rename/swap, helper-semantic drift, condition/data/reuse drift, and breaking result-schema evolution.

The workflow uses full Git history so permanent compatibility tests read immutable published and promotion-evidence source directly rather than trusting candidate-authored restatements.

### Exact record/document tree

State: **`NotRun` in this committed record.**

Reason: the commit containing this record does not exist until this file is committed. The successful functional run cannot substantiate this exact later tree. PR #71 therefore requires a fresh permanent CI run on the resulting exact head. That run belongs in PR metadata rather than this file.

## Compatibility evidence

Classification: **compatible relative to published `v0.10.0`.**

The RC gate preserves the contracts named by `RELEASE_POLICY.md` without imposing byte-for-byte immutability on broad package, tool, schema, or C# standards trees. Published paths, identifiers, rule meanings, tool behavior, finding semantics, and promoted C# normative statements remain protected while documented compatible editorial and additive evolution remains possible.

The initial stable package promise remains intentionally narrow:

- `languages/csharp`: **stable**
- every package not explicitly enumerated as stable remains outside the `1.0.0` stable-package promise

PowerShell and Terraform/OpenTofu remain `baseline` under their recorded maturity decisions. Repository path and pre-1.0 compatibility obligations do not imply stable package maturity.

## Migration exercise

Migration from published `v0.10.0` to this candidate was exercised as additive compatibility review. No required breaking migration was identified. Adopters evaluating the RC must pin the exact candidate source and rerun applicable validation. C# adopters must review the newly explicit stable commitment; Windows PowerShell 5.1 and mixed-profile adopters must review their additive overlays where applicable. Downgrade remains possible by restoring the published source pin and removing RC-only maturity claims.

## Review findings

PR #71 has received **thirty-one material P1 findings and two material P2 findings** during preparation. The remediations are durable in permanent regression coverage:

1. Candidate-only stable-path evidence was replaced with immutable published checkpoint evidence and removal regressions.
2. Tool compatibility expanded from path presence to CLI/result/exit/schema/overwrite behavior.
3. Exact-tree readiness was corrected from an earlier-run claim to truthful `NotRun` in the committed record.
4. Top-level SHA duplication was removed in favor of this readiness record as functional-evidence authority.
5. Published front-matter and schema identifiers were checkpointed with mutation/removal coverage.
6. Normative-rule coverage expanded beyond governance and C#.
7. Complete published numbered-rule coverage is derived from immutable source.
8. Public finding-code values and meanings were added to the compatibility gate.
9. `releases/README.md` exact-tree wording was qualified.
10. Root `README.md` exact-tree wording was qualified.
11. All 13 stable profile files plus all 13 package directories were checkpointed.
12. Finding codes were bound to published semantic emission context.
13. C# coverage expanded beyond ten summary rules to the promoted normative standards surface.
14. Finding semantics added referenced constants and dataflow.
15. `RELEASE_HEAD_TAG_MISSING` gained end-to-end helper-path coverage.
16. `MANIFEST.md` permanent-validation wording was qualified to the functional candidate.
17. The tagged-success `git_output(...)` path was added.
18. All seven published agent-skill entry points were independently anchored.
19. Broad normative tree hashes were removed in favor of semantic rule contracts.
20. Broad tool-directory hashes were removed in favor of semantic finding/tool contracts.
21. Same-file duplicate new rule IDs are rejected by occurrence counting.
22. Finding semantic signatures include referenced helper implementations, with the reviewed `read_release_state()` change narrowly pinned.
23. Result-schema compatibility became structural instead of whole-file hash equality.
24. Caller-supplied finding codes such as `LICENSE_ENCODING` became source-derived contracts.
25. Every published `--format` value is exercised across the stable CLI surface.
26. `ROADMAP.md` was qualified to distinguish functional-candidate validation from exact-tree validation.
27. Caller-supplied finding codes were bound to semantic call-site identity, including a code-swap regression.
28. Writer safety expanded from one representative writer to every published stable writer.
29. Literal finding reachability is now preserved, detecting unconditional preceding termination and fully terminating branches.
30. Parameterized finding helpers are now bound to their own normalized behavior semantics, detecting changes to the condition under which caller-supplied codes emit.
31. C# promoted standards now preserve front-matter IDs and normative Markdown contracts semantically instead of freezing the entire standards tree.
32. **P2:** human-readable finding message wording was removed from stable semantic identity while non-message semantics remain enforced.
33. **P2:** private parameter/local names are alpha-normalized so rename-only edits remain compatible while behavioral changes still fail.

A separate automated P1 asking the changelog to mark the already-merged C# stable promotion as pending was rejected as a gate-conflation false positive. PR #70 completed that maturity promotion and received independent specialist approval on its exact final head. PR #71's independent approval is a distinct RC compatibility gate.

Automated/repository review does **not** substitute for the independent specialist approval required by release policy. Any approval submitted before the latest remediation commits is historical only.

## Readiness state

| Gate | State | Evidence / limitation |
|---|---|---|
| Candidate version and notes | Passed | `VERSION`, `CHANGELOG.md`, `releases/1.0.0-rc.1.md` |
| Migration from published checkpoint | Passed | migration record and permanent regressions |
| Stable paths / agent-skill routes / identifiers | Passed | immutable checkpoint-derived permanent regressions in run `31989888372` |
| Complete published numbered normative-rule compatibility | Passed | source-derived path + ID + requirement occurrence comparison in run `31989888372` |
| C# stable identifiers and numbered product rules | Passed | explicit stable identifiers and product-rule meanings in run `31989888372` |
| Full C# promoted normative standards surface | Passed | promotion-evidence `*_STANDARD.md` IDs and normative contracts preserved semantically; compatible editorial/additive evolution allowed in run `31989888372` |
| Published tool-behavior compatibility | Passed | CLI option/value, result/exit, structural schema, and all-writer overwrite/dry-run/force exercises in run `31989888372` |
| Published finding-code compatibility | Passed | literal reachability, literal/caller-supplied semantics, parameterized-helper behavior, helper/data dependencies, runtime exercises, and negative regressions in run `31989888372` |
| Functional candidate permanent validation | Passed | run `31989888372` on `d70a4d3d96c3f3691b021f74e2e8d22b36114821` |
| Exact record/document tree permanent validation | `NotRun` | Requires fresh CI on the PR head created by this record commit |
| RC tag contract validation | Passed on functional candidate | permanent `validate_release.py --tag v1.0.0-rc.1` regression |
| Stable-package boundary | Passed | C# stable; non-enumerated packages excluded from stable promise |
| Independent specialist approval for current exact PR head | `NotRun` | Earlier approvals predate later P1/P2 remediations and are historical only |
| Published `v1.0.0-rc.1` tag | `NotRun` | No publication claim in this source record |
| Published GitHub Release and attached artifacts | `NotRun` | Requires deliberate tag-driven publication |
| Independent checksum/artifact verification after publication | `NotRun` | Cannot occur before publication |
| Final `1.0.0` exact-commit compatibility approval | `NotRun` | Separate final-release gate |
| GitHub-host controls previously recorded as manual/Blocked | `Blocked` where previously recorded | Repository-source validation cannot configure or prove those controls |

## Final 1.0.0 boundary

Final `1.0.0` remains blocked until all policy gates are satisfied, including a published `1.0.0-rc.N`, independent release-artifact/checksum verification, independent compatibility approval on the exact final release commit, no unresolved high/critical release blocker, and current source-review/security/adoption/maturity/ownership/migration evidence.

This readiness record establishes a validated functional compatibility candidate while leaving exact record/document-tree validation truthful. It does not convert a prepared RC into a published release and does not collapse the separate final-`1.0.0` approval gate.
