# 1.0.0-rc.1 Readiness Record

Status: **Functional compatibility candidate validated; exact record/document tree validation remains `NotRun` in this committed record; publication and exact-head independent specialist approval remain pending.**

This record captures repository-source compatibility evidence for `1.0.0-rc.1`. It does not claim that the `v1.0.0-rc.1` tag or GitHub Release exists, it does not claim final `1.0.0` readiness, and it does not claim that the commit containing this record has already been validated.

## Candidate identity

- Candidate version: `1.0.0-rc.1`
- Functional compatibility candidate commit: `bb61e5582cda3d8cffcdb97478ba8f3ec4f2ac1d`
- Functional validation run: `32024445942`
- Pull request: `#71` (`release: exercise 1.0.0-rc.1 compatibility gate`)
- Published migration checkpoint: `v0.10.0`
- Published checkpoint commit: `83c73f3ab9a049ff2321d463164fcf98fb453a9c`
- C# immutable promotion-evidence candidate: `2f6d39288e5c1a7d416e62cd75651b3d6da48dfe`
- Compatibility inventory: `releases/compatibility/1.0.0-rc.1.json`
- Published path and identifier checkpoint: `releases/compatibility/0.10.0-checkpoint.json`
- Published agent-skill entry-point checkpoint: `releases/compatibility/0.10.0-agent-skill-entrypoints.json`
- Published normative-rule checkpoint: `releases/compatibility/0.10.0-rule-contracts.json`
- Published tool-behavior checkpoint: `releases/compatibility/0.10.0-tool-behavior.json`
- Published finding-code checkpoint: `releases/compatibility/0.10.0-finding-codes.json`
- Migration exercise: `releases/migrations/1.0.0-rc.1.md`

The functional candidate intentionally precedes this readiness-record commit. The complete compatibility gate was executed on that immutable tree. Validation of the later commit containing this record must be established by a subsequent exact-head CI run and carried in PR metadata rather than written back here and creating a self-referential head.

## Permanent validation evidence

### Functional compatibility candidate

- GitHub Actions workflow: `Validate standards`
- Workflow run: `32024445942`
- Candidate commit validated: `bb61e5582cda3d8cffcdb97478ba8f3ec4f2ac1d`
- Result: **Passed**
- Runtime boundary: Ubuntu 24.04, Python 3.13, hash-locked validation dependencies
- Command: `python tools/validate-all/run_all.py --include-tests`
- Validators completed: repository standards, links, source freshness, skills, schemas, templates, tools, release validation, and unit tests

An earlier intermediate run, `31990518406`, failed because `test_rc_numbered_rule_semantics.py` was accidentally committed as a malformed payload rather than Python source. It was corrected before the validated functional lineage continued. Intermediate runs `32024083346` and diagnostic `32024241431` failed because the first alternate-root regression incorrectly assumed an empty directory must be invalid for `check-links`; an empty link set is a legitimate no-op. The clean candidate instead gives `check-links` a root-local broken link while other common tools exercise a missing-contract alternate root. No readiness claim is based on any failed run.

The permanent RC suite now verifies:

- stable root, schema, template, tool-entry, profile-file, and profile-directory paths against immutable `v0.10.0` evidence;
- all twelve published stable JSON Schema contracts, including rolling and `schemas/v1/` copies, are compared semantically against immutable `v0.10.0` source rather than protected only by path and `$id`; published required fields, existing property types, enums, extension behavior, and other assertions remain compatible while documented optional additions and annotations remain possible;
- the historical `1.0.0-rc.1` release-state gate is monotonic: before publication the repository may be prepared at rc.1, while after publication `rc.1` remains in `publishedVersions` and both `VERSION` and `nextIntendedVersion` must advance beyond it, so the permanent suite remains valid after publication;
- all seven published agent-skill routes, with the dedicated agent-skill checkpoint explicitly pinned and authenticated by the authoritative RC inventory;
- every machine-readable checkpoint digest in the RC inventory matches the exact referenced checkpoint file;
- stable template placeholder names, required level-2 sections, and published behavior-defining section obligations derived from immutable `v0.10.0` templates, including accountable-human exception approval and evidence-supported completion claims;
- published front-matter identifiers, schema/tool-result `$id` values, numbered rule IDs, and every published bold-labeled numbered-rule behavioral field such as `Requirement`, `Evidence`, `Applicability`, `Exceptions`, and `Expected evidence` when present;
- published scoped duplicate rule IDs exactly where released, while new cross-file or same-file reuse is rejected and new unique rules remain compatible additions;
- C# stable identifiers and numbered product-rule meanings;
- promoted C# `*_STANDARD.md` files by stable ID plus promoted normative Markdown contracts from immutable promotion evidence, including `Evidence` obligations, while genuinely editorial sections remain editable and additive compatible requirements remain possible;
- published CLI option names and values plus actual `--root`, `--output`, and `--quiet` behavior across every common stable CLI entry point: the real repository root succeeds, while a deliberately invalid alternate root must produce non-passing status, nonzero exit, and findings; `check-links` receives a root-local broken link because a truly empty link set is a valid no-op;
- every common stable tool's emitted JSON preserves all published required result fields, status values, required finding fields, and finding-severity values while still allowing compatible optional additions;
- both published output formats are exercised through real tool execution rather than `--help`: JSON is checked against the published required result contract, while text output is verified as the human-readable rendering of the same tool/status/summary/finding result;
- published result/exit-code contracts, release artifact naming, and structural result-schema compatibility;
- every published writer that exposes replacement behavior, including `generate-manifest`, `compose-agents`, and `build_release.py`, refuses an existing output without `--force` and replaces it only with `--force`; the two writers that publish `--dry-run` also retain dry-run non-mutation;
- every published finding code derived from literal and caller-supplied source contracts round-trips unchanged through shared `Finding.to_dict()` and `ToolResult.to_dict()` serialization;
- every published literal finding code remains a source-derived semantic contract and reachable finding occurrence, preventing preceding unconditional control flow from silently suppressing a published finding, including statically true literal/module/local feature-flag termination and statically infinite `while` loops without an outer-loop `break` path;
- caller-supplied finding-code call sites are also reachability-filtered, so a preceding `return`, `raise`, or statically infinite loop cannot leave a dead call site falsely counted as a preserved public contract;
- caller-supplied finding codes by source path, caller identity, resolved non-code arguments, helper identity, and code value, including a negative `LICENSE_ENCODING` / `NOTICE_ENCODING` call-site swap regression;
- module-level parameterized-finding helpers by alpha/message-normalized implementation semantics, so wrong exception/condition behavior fails while private renames and message rewording remain compatible;
- behavior-driving constants, local dataflow, referenced helper implementations, code identity, and stable non-message emission shape;
- the intentional post-`v0.10.0` `read_release_state()` strengthening pinned to immutable reconciliation commit `a96d6a92da40257cbe4c6e0fe0c7bbbd397adef3` only for its two affected publication findings;
- both runtime outcomes of `git_output(...)` for `RELEASE_HEAD_TAG_MISSING`;
- `validate_release.py --tag v1.0.0-rc.1` as a permanent RC-tag regression;
- negative regressions for post-publication release-state progression, all stable-schema contract drift, path/skill/template/identifier removal, checkpoint-digest drift, template obligation weakening, numbered-rule requirement/scope/exception/evidence drift, rule-ID reuse, literal and caller-supplied finding reachability loss, statically infinite-loop suppression, finding-code serialization drift, caller-code rename/swap, parameterized-helper drift, finding condition/data/reuse drift, C# evidence/requirement weakening, CLI root/option/format/result-contract behavior loss, writer-safety regression, and breaking result-schema evolution.

The workflow uses full Git history so compatibility tests read immutable published and promotion-evidence source directly rather than trusting candidate-authored restatements.

### Exact record/document tree

State: **`NotRun` in this committed record.**

Reason: the commit containing this record does not exist until this file is committed. The successful functional run cannot substantiate this exact later tree. PR #71 therefore requires a fresh permanent CI run on the resulting exact head. That later result belongs in PR metadata rather than this file.

## Compatibility evidence

Classification: **compatible relative to published `v0.10.0`.**

The RC gate preserves the public contracts named by `RELEASE_POLICY.md` without imposing broad byte-for-byte immutability. Published paths, schema semantics, identifiers, authenticated checkpoint references, template fields/sections/normative obligations, numbered-rule scope and obligations, tool CLI/filesystem/stdout/format/result behavior, finding construction/serialization/reachability semantics, and promoted C# normative/evidence obligations remain protected while documented compatible editorial and additive evolution remains possible. The historical RC-state tests are also forward-compatible with subsequent publication rather than permanently freezing the repository in pre-publication state.

The initial stable package promise remains intentionally narrow:

- `languages/csharp`: **stable**
- every package not explicitly enumerated as stable remains outside the `1.0.0` stable-package promise

PowerShell and Terraform/OpenTofu remain `baseline` under their recorded maturity decisions. Repository path and pre-1.0 compatibility obligations do not imply stable package maturity.

## Migration exercise

Migration from published `v0.10.0` to this candidate was exercised as additive compatibility review. No required breaking migration was identified. Adopters evaluating the RC must pin the exact candidate source and rerun applicable validation. C# adopters must review the newly explicit stable commitment; Windows PowerShell 5.1 and mixed-profile adopters must review their additive overlays where applicable. Downgrade remains possible by restoring the published source pin and removing RC-only maturity claims.

## Review findings

PR #71 has received **forty-seven material P1 findings and three material P2 findings** during preparation. The latest P1 remediations are:

43. The permanent RC release-state gate is now monotonic across publication: it validates the pre-publication rc.1 state today and, once rc.1 is published, requires `publishedVersions` to retain rc.1 while `VERSION` and `nextIntendedVersion` advance beyond it instead of failing forever.
44. Every one of the twelve published stable schemas is now compared structurally against immutable `v0.10.0` schema source, not merely checked for path and `$id` preservation.
45. Common CLI `--root` behavior now requires a deliberately invalid alternate root to produce nonzero/non-passing validation with findings; the link checker receives a root-local broken link because an empty link set is legitimately valid.
46. Caller-supplied finding-code call sites are now filtered by reachability so dead calls after `return`, `raise`, or statically infinite flow cannot satisfy the compatibility gate.
47. Literal finding reachability now treats statically true `while` loops without an outer-loop `break` as terminating control flow, including the reviewed `while True: continue` case, while a possible outer-loop break keeps following code reachable.

The immediately preceding P1 remediations added release-builder overwrite protection, stable-template normative obligations, public finding-code serialization round-trips, checkpoint digest integrity, emitted JSON result validation, real text/JSON rendering, constant-condition reachability, parameterized-helper semantics, semantic C# promotion coverage, complete numbered-rule behavioral fields, C# evidence obligations, stable template path/section contracts, and behavioral root/output/quiet CLI coverage. Earlier P1 remediations cover immutable stable-path evidence, tool behavior, truthful exact-tree validation state, top-level traceability, front-matter/schema IDs, complete published numbered-rule coverage, public finding-code values and semantics, release/readiness wording, all profile entry points, complete C# promotion coverage, referenced data/helper behavior, both tag-helper outcomes, all agent-skill entry points, removal of broad package/tool byte-identity constraints, same-file rule-ID reuse, structural result-schema compatibility, caller-supplied finding-code discovery/call-site semantics, roadmap qualification, and writer overwrite/dry-run/force behavior.

The three P2 remediations preserve compatible evolution by excluding human-readable finding messages from semantic identity, alpha-normalizing private parameter/local names while keeping behavior-driving semantics enforced, and making the agent-skill checkpoint discoverable/authenticated through the authoritative machine-readable RC inventory.

A separate automated P1 asking the changelog to mark the already-merged C# stable promotion as pending was rejected as a gate-conflation false positive. PR #70 completed that maturity promotion and received independent specialist approval on its exact final head. PR #71's independent approval is a distinct RC compatibility gate.

Automated/repository review does **not** substitute for the independent specialist approval required by release policy. Any approval submitted before the latest remediation commits is historical only.

## Readiness state

| Gate | State | Evidence / limitation |
|---|---|---|
| Candidate version and notes | Passed | `VERSION`, `CHANGELOG.md`, `releases/1.0.0-rc.1.md` |
| Migration from published checkpoint | Passed | migration record and permanent regressions |
| Historical RC-state progression | Passed | pre-publication state is valid now; post-publication progression is monotonic and remains permanently testable in run `32024445942` |
| Stable paths / agent-skill routes / identifiers | Passed | immutable checkpoint-derived permanent regressions in run `32024445942`; agent-skill checkpoint is RC-inventory pinned |
| Published stable schema compatibility | Passed | all twelve stable rolling/versioned schemas compared semantically to immutable `v0.10.0` source in run `32024445942` |
| Machine-readable checkpoint digest integrity | Passed | all RC inventory checkpoint references match referenced file SHA-256 values in run `32024445942` |
| Stable template content contracts | Passed | immutable placeholder, required-section, and normative-obligation comparison in run `32024445942` |
| Complete published numbered normative-rule compatibility | Passed | source-derived IDs plus all published labeled behavioral fields in run `32024445942` |
| C# stable identifiers and promoted normative/evidence obligations | Passed | semantic promotion-evidence comparison in run `32024445942` |
| Published common CLI behavior | Passed | all common tools exercise root/output/quiet effects plus real text/json rendering; deliberately invalid alternate roots fail with findings in run `32024445942` |
| Published JSON result contract | Passed | emitted common-tool payloads preserve published required result/finding fields and enums in run `32024445942` |
| Published tool/result/writer compatibility | Passed | result/exit/schema plus overwrite safety for generate-manifest, compose-agents, and release builder; dry-run coverage where published, in run `32024445942` |
| Published finding-code compatibility | Passed | literal/caller-supplied semantics, shared serialization round-trip, extended reachability including infinite loops, parameterized-helper behavior, helper/data dependencies, runtime exercises, and negative regressions in run `32024445942` |
| Functional candidate permanent validation | Passed | run `32024445942` on `bb61e5582cda3d8cffcdb97478ba8f3ec4f2ac1d` |
| Exact record/document tree permanent validation | `NotRun` | Requires fresh CI on the PR head created by this record commit |
| RC tag contract validation | Passed on functional candidate | permanent `validate_release.py --tag v1.0.0-rc.1` regression |
| Stable-package boundary | Passed | C# stable; non-enumerated packages excluded from stable promise |
| Independent specialist approval for current exact PR head | `NotRun` | Earlier approvals predate later P1/P2 remediations and are historical only |
| Published `v1.0.0-rc.1` tag | `NotRun` | No publication claim in this source record |
| Published GitHub Release and attached artifacts | `NotRun` | Requires deliberate tag-driven publication |
| Independent checksum/artifact verification after publication | `NotRun` | Cannot occur before publication |
| Final `1.0.0` exact-commit compatibility approval | `NotRun` | Separate final-release gate |
| GitHub-host controls previously recorded as manual/Blocked | `Blocked` where previously recorded | Repository-source validation cannot configure or prove those controls |

## Final 1.0.0 boundary

Final `1.0.0` remains blocked until all policy gates are satisfied, including a published `1.0.0-rc.N`, independent release-artifact/checksum verification, independent compatibility approval on the exact final release commit, no unresolved high/critical release blocker, and current source-review/security/adoption/maturity/ownership/migration evidence.

This readiness record establishes a validated functional compatibility candidate while leaving exact record/document-tree validation truthful. It does not convert a prepared RC into a published release and does not collapse the separate final-`1.0.0` approval gate.
