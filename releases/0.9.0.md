# Public-Access-Agents 0.9.0

## Publication state

- Repository maturity: pre-1.0 prepared baseline
- Compatibility class: initial repository-level compatibility baseline
- Intended tag: `v0.9.0`
- Prepared: 2026-07-13
- Published tag: **None**
- GitHub Release: **None**
- License: Apache-2.0

These materials were prepared as the first repository-wide release program, but `0.9.0` was never published. GitHub has no `v0.9.0` tag and no `0.9.0` GitHub Release.

Current `main` contains substantial work accumulated after this prepared baseline and **must not** be retroactively tagged as `v0.9.0`. The intended next publication is the forward-only `0.10.0` release after its version, notes, migration guidance, artifacts, and validation evidence are reconciled.

This prepared baseline established the repository-wide versioning, changelog, release policy, maturity policy, release tooling, checksum format, and tag-driven GitHub Release process as repository content. It does not establish a published source boundary and does not establish the final `1.0.0` compatibility promise.

## Breaking changes

None.

This was prepared as the first repository-level release contract. Existing public paths, rule identifiers, schemas, templates, and executable entry points form the initial pre-1.0 baseline described by [`RELEASE_POLICY.md`](../RELEASE_POLICY.md), but no published `v0.9.0` tag exists to identify an immutable source commit.

## Normative changes

- Established repository-wide Semantic Versioning rules for normative standards, schemas, templates, tools, stable paths, governance, and release artifacts.
- Defined patch, minor, major, and pre-1.0 classification behavior.
- Defined deprecation windows of at least 90 calendar days for ordinary contracts and 180 calendar days for stable paths and machine-readable contracts.
- Required migration notes for every release.
- Defined the `1.0.0` compatibility gate and release-candidate expectation.
- Added package maturity states and baseline-to-stable promotion requirements.
- Required maturity review records, adoption evidence, compatibility inventory, source review, and independent specialist review for stable promotion.

## Editorial changes

- Added release and maturity navigation to root documentation.
- Added version-specific release and migration guides.
- Clarified that a repository version does not automatically promote every package to stable.

## Tooling changes

- Added release-policy validation to the unified validation pipeline.
- Added deterministic ZIP and TAR.GZ release archive generation.
- Added `SHA256SUMS.txt` generation.
- Added `release-manifest.json` with source commit, artifact sizes, and SHA-256 digests.
- Added tag validation and tag-driven GitHub Release publication.
- Added regression tests for invalid versions, missing release notes, tag mismatches, and incomplete release policy.

## Deprecations

None.

## Migration notes

See [`migrations/0.9.0.md`](migrations/0.9.0.md).

Existing adopters who used repository content from this period should record the exact source commit they actually consumed. They must not claim adoption of a published `v0.9.0` tag because that tag was never created.

## Security

- The prepared release process requires permanent repository validation before artifact creation.
- Release tags must match the canonical `VERSION` file.
- Release archives are built from Git-tracked files with deterministic ordering and normalized metadata.
- SHA-256 checksums and a release manifest are generated with each release build.
- The release workflow scopes `contents: write` to publication rather than validation/build work.

These controls describe the prepared release mechanism. They are not evidence that a `0.9.0` publication occurred.

## Known limitations

- `0.9.0` was never published as a Git tag or GitHub Release.
- There is no immutable `v0.9.0` source boundary to recover from current `main` without deliberately selecting a historical commit.
- Most standards packages remain at `baseline`, not `stable`.
- Representative package-level adoption tests are not yet complete.
- The repository currently has one active maintainer.
- Independent specialist coverage remains incomplete for governance, security, schemas, tools, and release automation.
- GitHub ruleset and branch-protection settings require separate administrative verification.
- Release artifacts do not certify adopting projects.

## Validation

The following commands describe how the prepared `0.9.0` program was intended to be validated before publication:

```bash
python -m pip install -r tools/validate-schemas/requirements.txt
python tools/validate-all/run_all.py --include-tests
python tools/release/validate_release.py --tag v0.9.0
python tools/release/build_release.py --tag v0.9.0 --output-dir dist --force
```

They are retained as historical preparation guidance only. **Do not run them against current `main` as authority to create `v0.9.0`.** Current `main` is a later repository state and the next intended publication is `0.10.0`.

## Review requirement

The prepared release program changed governance, compatibility, executable tooling, and GitHub Actions behavior. A future publication must satisfy the review requirements applicable to that publication at the selected source commit.
